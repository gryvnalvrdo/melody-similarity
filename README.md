# melody-similarity
A melody similarity search system using a Demucs + CQT-based CRNN (CNN + Bi-LSTM) model trained with NT-Xent contrastive loss. Extracts 256-dim melody embeddings from audio for efficient nearest-neighbor retrieval.
